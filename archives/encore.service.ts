import { Service } from "encore.dev/service";

// Encore will consider this directory and all its subdirectories as part of the "archives" service.

/**
 * Facilitates the extraction and storage of raw civic data from public sources.
 */
export default new Service("archives");
